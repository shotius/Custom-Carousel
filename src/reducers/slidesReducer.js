export const initialState = {
    slides: [1,2,3,4,5],
    curIndex: 0,
}

export const slidesReducer = (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}